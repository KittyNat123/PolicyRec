# PolicyRec 구현 로직 · 문구 검증 · 발표 회고 정리 v1

## 0. 기준 버전

- GitHub HEAD 기준 커밋:
  - `HEAD=b40ec73 chore: 사용자 화면 정리`
- 로컬 변경사항:
  - `web/app/page.tsx`
  - `web/app/api/search/route.ts`
- HEAD 기준과 로컬 데모 기준 차이:
  - GitHub HEAD 기준은 사용자 화면 정리 커밋까지의 구현 상태를 기준으로 한다.
  - 로컬 데모 기준은 위 두 파일의 수정이 추가로 섞인 상태다.
  - 특히 로컬 변경은 검색어가 있는 상태의 마감순/신규순 정렬을 클라이언트 내부 정렬에서 서버 재검색 방식으로 바꾸고, 전세사기/창업 키워드 보조 컷을 추가해 데모 품질을 보완한 상태다.
  - 따라서 발표에서는 기능 설명을 할 때 `GitHub HEAD 기준`과 `로컬 데모 기준`을 구분해야 한다.
- 이 문서의 목적:
  - PolicyRec의 데이터 수집, 정제, DB 저장, 검색, 추천, 챗봇, 스크랩, 관리자 화면까지의 구현 흐름을 발표 준비용으로 정리한다.
  - 실제 구현과 화면 문구가 어긋나는 부분을 검증하고, 발표에서 사용할 수 있는 표현과 피해야 할 표현을 구분한다.
  - 20분 발표자료, 데모 시나리오, 회고 슬라이드에 바로 옮길 수 있는 형태로 핵심 내용을 구조화한다.

## 1. 전체 시스템 흐름

PolicyRec의 전체 흐름은 다음과 같이 설명할 수 있다.

```text
데이터 소스 API
→ raw JSON 저장
→ Python 정규화/검수/notebook
→ Gemini embedding 생성
→ Supabase announcements 저장
→ Next.js API Route
→ 웹 화면 검색/추천/챗봇/스크랩/관리자 표시
```

### 전체 흐름 설명

- 데이터 소스 API:
  - 온통청년, 기업마당, K-Startup 3가지 공공 정책/지원사업 데이터를 사용한다.
  - 각 소스는 응답 구조와 필드명이 달라서 그대로 화면에 쓰기 어렵다.
- raw JSON 저장:
  - `scripts/fetch.py`와 `app/collectors/*.py` 계열 수집 코드가 외부 API 응답을 가져오는 역할을 한다.
  - 수집된 원천 데이터는 정규화 전 단계에서 raw 형태로 보존될 수 있다.
- Python 정규화/검수/notebook:
  - `app/norm.py`에서 서로 다른 API 필드를 공통 정책 공고 스키마로 맞춘다.
  - `policyRec_v1_2_2.ipynb`, `policyRec_v1_2_3.ipynb`는 데이터 검수, 정제, 임베딩 생성, Supabase 업로드 흐름을 실험하고 실행하는 역할을 한다.
- Gemini embedding 생성:
  - 검색어와 공고 텍스트를 의미 벡터로 바꾸는 데 Gemini embedding을 사용한다.
  - 통합검색에서는 검색어 embedding을 만들고, Supabase RPC에서 공고 embedding과 유사도를 계산한다.
- Supabase `announcements` 저장:
  - 정규화된 정책 공고는 Supabase의 `announcements` 테이블에 저장된다.
  - 제목, 요약, 기관, 분야, 지역, 지원 내용, 신청 방법, 제출 서류, 추가 조건, 대상, 상세 URL 등 화면에서 쓰는 상세 필드가 이 테이블과 연결된다.
- Next.js API Route:
  - 웹 화면은 DB에 직접 접근하지 않고 Next.js API Route를 통해 검색, 추천, 챗봇, 스크랩, 관리자 데이터를 요청한다.
  - 대표적으로 `/api/search`, `/api/mypage/recommendations`, `/api/chat/rag`, `/api/mypage/scraps`, `/api/mypage/info`가 있다.
- 웹 화면 연결 방식:
  - `web/app/page.tsx`가 통합검색, 조건검색, 공고 카드, 더보기, 일부 마이페이지/관리자 표시 흐름과 연결된다.
  - 사용자가 검색어, 필터, 정렬 옵션을 바꾸면 API Route로 payload를 보내고, 응답 결과를 카드 UI로 렌더링한다.

### 발표 시 주의할 점

- 완전 자동 배치 시스템이라고 말하면 안 된다.
- 현재 수집, 정제, 임베딩 생성, 업로드는 수동 실행 성격이 강하다.
- `api_batch_logs`는 테이블/API 조회 흔적은 있지만, 수집 스크립트가 실제로 자동 로그를 쓰는 배치 자동화까지 확정된 상태로 말하면 안 된다.
- 발표에서는 “데이터 수집과 정제 파이프라인을 구성했고, notebook과 Python 스크립트를 통해 Supabase에 적재했다” 정도가 정확하다.

### 사용한 리트리버 구조

- 결론부터 말하면, **Supabase 기반 retriever**라고는 말할 수 있지만 **Supabase Retriever 라이브러리를 그대로 사용한 것은 아니다**.
- 코드에서는 LangChain `Retriever`, `SupabaseVectorStore`, `asRetriever()` 같은 라이브러리형 retriever를 붙인 흔적이 없다.
- 대신 Next.js API Route 안에서 Gemini embedding을 만들고, Supabase PostgreSQL/pgvector RPC를 직접 호출하는 **Supabase RPC 기반 자체 hybrid retrieval layer**로 구현되어 있다.
- 즉 “Supabase에서 제공하는 retriever 객체를 썼다”기보다, “Supabase DB의 vector search 기능을 직접 호출하도록 retriever 역할을 로컬 API Route에서 구성했다”가 가장 정확하다.

#### Supabase와 로컬 구현의 역할 분리

| 구분 | 역할 | 관련 파일 | 설명 |
|---|---|---|---|
| Gemini embedding | 검색어/질문/추천 query를 vector로 변환 | `web/lib/gemini.ts` | `gemini-embedding-001`로 768차원 embedding을 만든다. |
| Supabase DB/pgvector | 공고 embedding과 query embedding의 유사도 계산 | `Database/RPC_match_announcements_hybrid_v1.sql`, `Database/supabase_table_create_query.txt` | `announcements.embedding`과 pgvector 연산으로 후보를 찾는다. |
| Supabase RPC | vector search를 DB 함수로 실행 | `match_announcements_hybrid`, fallback `match_announcements` | API Route에서 RPC를 직접 호출한다. |
| Next.js API Route | query 구성, RPC 호출, fallback, 점수 보정, 응답 정리 | `web/app/api/search/route.ts`, `web/app/api/chat/rag/route.ts`, `web/app/api/mypage/recommendations/route.ts` | retriever orchestration을 로컬 코드에서 직접 담당한다. |
| LangChain/SupabaseVectorStore retriever | 사용하지 않음 | 해당 없음 | 라이브러리형 retriever 객체를 생성해서 쓰는 구조가 아니다. |

- 통합검색과 챗봇 RAG의 1차 검색은 다음 흐름이다.
  - 사용자 검색어/질문을 Gemini embedding으로 변환
  - Supabase RPC `match_announcements_hybrid`에 query embedding 전달
  - `announcements`의 공고 embedding과 vector similarity 계산
  - 지역/나이 조건 등 일부 조건 보정 후 결과를 카드 또는 챗봇 답변에 사용
- RPC SQL 기준 파일은 `Database/RPC_match_announcements_hybrid_v1.sql`로 정리되어 있고, API 호출 쪽에서는 `match_announcements_hybrid` RPC를 사용한다.
- fallback retriever도 있다.
  - hybrid RPC가 실패하면 기존 `match_announcements` RPC 또는 table filter 기반 조회로 fallback한다.
  - 조건검색처럼 검색어가 없는 경우에는 embedding retriever가 아니라 Supabase table filter + `condition_score` 정렬 흐름을 사용한다.
- AI 추천에서는 같은 hybrid retrieval 계열을 재사용하되, 검색 query를 사용자 프로필 조건 또는 최근 스크랩 제목/요약에서 만든다.
- 챗봇에서는 `/api/chat/rag` 안에서 사용자 질문, 프로필 조건, 현재 대화 맥락을 바탕으로 hybrid retriever를 호출하고, 검색된 공고를 Gemini 답변 생성의 근거로 넣는다.

#### 발표용 멘트

“강사님이 물어보셨던 리트리버 기준으로 말씀드리면, Supabase 기반은 맞지만 Supabase Retriever 라이브러리를 그대로 쓴 것은 아닙니다. 저희는 검색어와 질문을 Gemini embedding으로 바꾸고, Supabase의 pgvector RPC를 직접 호출해서 관련 공고를 가져오도록 구성했습니다. 벡터 유사도 계산은 Supabase DB에서 수행하고, 어떤 query를 만들지, 조건을 어떻게 섞을지, 실패했을 때 fallback을 어떻게 탈지는 Next.js API Route에서 직접 제어했습니다. 그래서 가장 정확하게는 ‘Supabase pgvector RPC 기반 자체 hybrid retriever’라고 설명할 수 있습니다.”

#### 발표에서 말해도 되는 표현

- “Supabase DB의 pgvector 검색 기능을 활용해서 공고 embedding과 query embedding의 유사도를 계산했습니다.”
- “별도 retriever 라이브러리를 붙인 것이 아니라, Next.js API Route에서 Supabase RPC를 직접 호출하는 retrieval layer를 구성했습니다.”
- “통합검색, AI 추천, 챗봇은 모두 검색어 또는 사용자 조건을 embedding query로 만들고 Supabase RPC에서 후보를 가져오는 흐름을 공유합니다.”
- “조건검색처럼 검색어가 없는 경우에는 embedding retriever가 아니라 DB 필터와 조건 점수 중심으로 후보를 정렬합니다.”

#### 조심해야 할 표현

- “Supabase Retriever를 그대로 사용했다”는 표현은 피하는 것이 좋다. 공식/라이브러리형 retriever 객체를 사용한 구조가 아니다.
- “전부 로컬에서 벡터 검색을 했다”도 부정확하다. 벡터 유사도 계산은 Supabase DB/RPC가 담당한다.
- “LangChain RAG로 구현했다”는 표현도 피한다. RAG 형태의 흐름은 있지만 LangChain retriever 체인을 사용한 것은 아니다.

## 2. 데이터 수집·정제·DB 저장 흐름

| 단계 | 설명 | 관련 파일 | 구현 상태 | 발표에서 말할 포인트 | 주의점 |
|---|---|---|---|---|---|
| 외부 API 수집 진입점 | 여러 정책 데이터 소스에서 공고 데이터를 가져오는 실행 진입점이다. | `scripts/fetch.py` | 구현됨 | “외부 정책 API에서 원천 데이터를 가져오는 수집 스크립트를 구성했습니다.” | 완전 자동 스케줄러로 실행된다고 말하지 말 것. |
| 기업마당 수집 | 기업마당 지원사업 데이터를 가져오고 후속 정규화 대상으로 넘긴다. | `app/collectors/biz.py` | 구현됨 | “기업 대상 지원사업 데이터도 통합 공고 데이터로 흡수했습니다.” | 기업마당 원본 필드와 최종 DB 필드는 다를 수 있다. |
| K-Startup 수집 | 창업 지원사업 데이터를 가져오는 collector다. | `app/collectors/kst.py` | 구현됨 | “창업 관련 공고는 K-Startup 데이터를 별도 collector로 수집했습니다.” | 창업 키워드 데모 품질은 로컬 변경에서 보조 컷이 추가된 상태임을 구분할 것. |
| 온통청년 수집 | 청년 정책 데이터를 가져오는 collector다. | `app/collectors/youth.py` | 구현됨 | “청년 정책 데이터는 온통청년 API를 통해 수집했습니다.” | 청년 대상 여부를 모든 공고에서 완벽한 hard filter로 보장한다고 말하지 말 것. |
| 공통 스키마 정규화 | 서로 다른 API 응답을 제목, 기관, 지역, 분야, 기간, 대상, 상세 내용 등 공통 구조로 맞춘다. | `app/norm.py` | 구현됨 | “서로 다른 API 응답을 하나의 공고 스키마로 정규화했습니다.” | 정규화는 데이터 품질에 영향을 받기 때문에 일부 필드는 비어 있거나 추론적일 수 있다. |
| 데이터 검수 및 변환 | 정규화 결과를 점검하고, 임베딩 생성 및 DB 적재 전 데이터를 확인한다. | `policyRec_v1_2_2.ipynb` | 구현/실행 성격 | “notebook에서 정규화 결과를 검수하고 업로드 전 데이터를 확인했습니다.” | 운영 자동화 코드라기보다 실험·검수·실행 notebook에 가깝다. |
| 임베딩 및 업로드 흐름 | Gemini embedding 생성, Supabase 업로드, 검색 품질 확인 흐름을 다룬다. | `policyRec_v1_2_3.ipynb` | 구현/실행 성격 | “공고 텍스트를 embedding으로 변환해 의미 검색이 가능하도록 했습니다.” | GPU 학습 파이프라인이 아니라 외부 embedding API를 활용한 구조다. |
| Supabase 테이블 생성 | `announcements` 등 서비스에 필요한 테이블 구조를 정의한다. | `Database/supabase_table_create_query.txt` | 구현됨 | “정규화된 공고와 사용자 관련 데이터를 Supabase 테이블에 저장했습니다.” | 테이블이 존재한다고 해서 모든 운영 기능이 완성된 것은 아니다. |
| Hybrid 검색 RPC | embedding similarity와 조건 보정을 함께 쓰는 검색 RPC다. | `Database/RPC_match_announcements_hybrid_v1.sql` | 구현됨 | “Supabase RPC로 vector similarity 기반 검색을 수행하고, API Route에서 조건 점수를 보정했습니다.” | RPC 실패 시 fallback이 있으므로 항상 RPC만 사용한다고 말하지 말 것. |

## 3. 웹 주요 기능 구현 흐름

### 3-1. 통합검색

- 실제 구현 흐름:
  - 사용자가 검색창에 검색어를 입력하면 `query` state가 변경된다.
  - 검색 버튼 또는 엔터 동작으로 `handleSearch()`가 실행된다.
  - `handleSearch()`는 `/api/search`로 검색어, 필터, 정렬 옵션, 사용자 조건 등을 담은 payload를 보낸다.
  - `/api/search`는 검색어가 있을 때 Gemini embedding을 생성한다.
  - 생성된 embedding을 Supabase RPC에 전달해 `announcements`의 공고 embedding과 similarity를 계산한다.
  - RPC가 실패하거나 결과가 부족한 경우 fallback 검색을 사용한다.
  - 응답 결과에는 similarity와 지역/나이 조건 보정이 반영된다.
  - 웹 화면은 API 응답을 받아 공고 카드 목록으로 렌더링한다.
- 관련 파일:
  - `web/app/page.tsx`
  - `web/app/api/search/route.ts`
  - `Database/RPC_match_announcements_hybrid_v1.sql`
- 사용 DB/API:
  - Gemini embedding API
  - Supabase `announcements`
  - Supabase RPC `match_announcements_hybrid`
  - Next.js API Route `/api/search`
- 발표용 멘트:
  - “통합검색은 사용자가 입력한 검색어를 중심으로 공고를 찾는 흐름입니다. 먼저 검색어를 Gemini embedding으로 바꾸고, 그 embedding을 Supabase의 `match_announcements_hybrid` RPC에 보내서 공고 embedding과 가까운 후보를 가져옵니다.”
  - “여기서 지역이나 나이 보정이 들어간다는 말은, 로그인한 사용자의 프로필이 항상 자동으로 들어간다는 뜻은 아닙니다. 화면에서 지역이나 나이 조건이 함께 전달됐거나, 검색어 안에서 ‘부산’, ‘25세’처럼 비교적 명확한 조건을 읽어낸 경우 그 조건을 보조 점수로 활용한다는 뜻입니다.”
  - “fallback 검색은 주 검색 경로가 실패했을 때를 대비한 예비 검색입니다. 먼저 풍부한 컬럼을 반환하는 hybrid RPC를 시도하고, 그 RPC가 실패하면 기존 `match_announcements` RPC로 유사한 공고 id와 similarity를 가져옵니다. 이후 `announcements` 테이블에서 제목, 기관, 지원 내용 같은 상세 컬럼을 다시 조회해 화면 카드에 필요한 정보를 보강합니다.”
  - “만약 상세 컬럼 보강까지 실패하면 최소한 basic RPC 결과만이라도 반환하도록 되어 있습니다. 그래서 검색 기능 전체가 바로 끊기지 않게 1차 hybrid 검색, 2차 basic RPC, 3차 테이블 보강이라는 방어 흐름을 둔 구조라고 설명하면 됩니다.”
- 발표에서 말해도 되는 표현:
  - “통합검색은 검색어를 embedding으로 바꾼 뒤, 공고 embedding과의 유사도를 기준으로 관련 공고를 찾습니다.”
  - “검색어 자체의 의미 유사도를 기본으로 하고, 화면에서 전달된 조건이나 검색어에서 읽어낸 명확한 지역/나이 조건이 있으면 보조 점수로 반영합니다.”
  - “주 RPC가 실패할 때를 대비해 기존 RPC와 테이블 조회를 조합한 fallback 경로도 둔 구조입니다.”
- 조심해야 할 표현:
  - “검색어에서 지역, 분야, 나이를 모두 추출해 정확히 필터링한다”는 표현은 부정확하다.
  - 분야는 self-query로 추출될 수 있어도 통합검색 final_score에 직접 hard filter나 가점으로 들어간다고 말하면 안 된다.
  - “자연어 의도를 완벽히 이해한다”는 표현도 피해야 한다.
- 구현 상태:
  - HEAD 기준으로 통합검색, embedding, Supabase RPC, fallback, 카드 렌더링이 구현되어 있다.
  - 로컬 데모 기준으로 검색어가 있는 날짜 정렬 품질 보완이 추가되어 있다.

#### HEAD와 로컬 차이

- GitHub HEAD 기준:
  - 검색어가 있는 상태에서 마감순/신규순을 선택하면 이미 받아온 10개 결과 안에서 클라이언트 날짜 정렬을 한다.
  - 이 때문에 검색어 관련 후보가 충분히 넓게 잡히지 않으면 날짜 정렬 데모 품질이 흔들릴 수 있다.
- 로컬 데모 기준:
  - 검색어가 있는 날짜 정렬도 서버에 `sort_by`를 보낸다.
  - 서버에서 관련 후보 50개를 넓게 가져온 뒤, 관련도 floor를 적용하고 날짜 정렬을 수행한다.
  - 전세사기/창업 키워드 보조 컷도 추가되어 데모에서 더 안정적인 결과를 보여줄 수 있다.

### 3-2. 조건검색/필터

- 실제 구현 흐름:
  - 사용자가 지역, 카테고리, 대상, 나이, 정렬 옵션을 선택한다.
  - 선택 조건이 `/api/search` payload에 포함된다.
  - 지역과 카테고리는 DB 조회 단계에서 hard filter 성격으로 반영된다.
  - 대상 필터는 대부분 hard filter가 아니다.
  - 청년/시니어 대상은 나이 조건으로 보조 반영될 수 있다.
  - 조건검색 추천순은 embedding similarity보다 선택 조건 일치도 중심으로 정렬된다.
- 관련 파일:
  - `web/app/page.tsx`
  - `web/app/api/search/route.ts`
- 사용 DB/API:
  - Supabase `announcements`
  - Next.js API Route `/api/search`
- 발표용 멘트:
  - “조건검색은 통합검색처럼 문장을 embedding해서 의미적으로 비슷한 공고를 찾는 흐름이라기보다, 사용자가 직접 선택한 지역, 분야, 나이 조건으로 후보를 좁히는 흐름입니다.”
  - “지역과 분야는 DB 조회 단계에서 비교적 명확한 필터로 들어갑니다. 예를 들어 서울과 창업을 고르면, 해당 조건에 맞는 공고를 우선적으로 가져오는 식입니다.”
  - “대상 필터는 조금 조심해서 설명해야 합니다. 청년이나 시니어처럼 나이와 연결되는 대상은 나이 조건으로 보조 반영될 수 있지만, 소상공인이나 여성 같은 모든 대상 유형이 같은 수준의 DB hard filter로 정확히 걸러지는 구조는 아닙니다.”
  - “그래서 화면에서는 대상 필터를 ‘사용자 유형을 빠르게 고르는 UI’ 정도로 설명하고, 실제 정확한 연령 비교는 나이 입력값과 공고의 연령 조건을 비교하는 쪽에서 이뤄진다고 말하는 게 안전합니다.”
- 발표에서 말해도 되는 표현:
  - “조건검색은 사용자가 직접 고른 지역, 분야, 나이 조건을 중심으로 후보를 좁히는 검색입니다.”
  - “지역과 분야는 DB 필터로 적용하고, 나이 조건은 공고의 연령 조건과 비교해 보정합니다.”
- 조심해야 할 표현:
  - “대상 필터가 모든 대상 유형을 DB에서 정확히 필터링한다”는 표현은 피해야 한다.
  - “다중 누적 필터로 모든 조건이 AND 처리된다”는 식으로 설명하면 안 된다.
  - “청년/시니어/기업/소상공인/여성 등 모든 대상이 같은 수준의 hard filter로 적용된다”는 표현도 피해야 한다.
- 구현 상태:
  - 지역 필터, 카테고리 필터, 대상 필터 UI, 나이 조건, 정렬 옵션이 구현되어 있다.
  - 다만 대상 필터는 대부분 hard filter가 아니라 보조 조건 또는 UI 조건에 가깝다.

### 3-3. 추천순/마감순/신규순 정렬

- 실제 구현 흐름:
  - 통합검색 추천순:
    - 검색어 embedding similarity 기반으로 관련 공고를 찾는다.
    - 지역 exact match에 `+30` 보정이 들어간다.
    - 나이 조건 일치에 `+10` 보정이 들어간다.
    - 분야 가점은 통합검색 final_score에 직접 들어가지 않는다.
  - 조건검색 추천순:
    - `condition_score = 지역 30 + 분야 20 + 명시 나이 10` 구조로 조건 적합도를 계산한다.
    - 동점일 때는 마감일이 빠른 순으로 정렬한다.
  - 마감순:
    - 마감일이 가까운 공고를 먼저 보여준다.
  - 신규순:
    - 시작일 또는 등록 기준으로 더 최근 공고를 먼저 보여주는 방향의 날짜 정렬이다.
- 관련 파일:
  - `web/app/page.tsx`
  - `web/app/api/search/route.ts`
- 사용 DB/API:
  - Supabase `announcements`
  - Supabase RPC
  - Next.js API Route `/api/search`
- 발표용 멘트:
  - “정렬은 통합검색과 조건검색에서 의미가 조금 다릅니다. 통합검색 추천순은 검색어와 공고가 얼마나 의미적으로 가까운지를 먼저 보고, 그다음 검색어에서 읽어낸 지역이나 나이 조건, 또는 화면에서 함께 전달된 조건이 있으면 보조 점수로 반영합니다.”
  - “반대로 조건검색 추천순은 검색어 의미 유사도보다 사용자가 직접 고른 조건에 얼마나 잘 맞는지가 중심입니다. 내부적으로는 지역 조건, 분야 조건, 명시적으로 입력한 나이 조건에 점수를 주고, 점수가 같으면 마감일이 가까운 공고를 먼저 보여주는 방식입니다.”
  - “마감순과 신규순은 추천순과 목적이 다릅니다. 마감순은 신청 마감이 가까운 공고를 먼저 보는 용도이고, 신규순은 최근 시작되었거나 등록된 공고를 먼저 보는 용도입니다.”
  - “데모에서는 자연어 문장인 ‘부산에서 전세피해 지원받을 수 있을까’로 추천순 흐름만 보여주고, 마감순/신규순 비교는 서울, 창업, 25세처럼 조건이 명확한 조합으로 보여주는 것이 안정적입니다.”
- 발표에서 말해도 되는 표현:
  - “통합검색 추천순은 검색어 의미 유사도를 중심으로 하고, 명확한 지역/나이 조건이 있으면 보조적으로 반영합니다.”
  - “조건검색 추천순은 사용자가 고른 조건과 잘 맞는 공고를 먼저 보여주는 방식입니다.”
- 조심해야 할 표현:
  - “분야가 통합검색 점수에 직접 가점으로 들어간다”는 표현은 피해야 한다.
  - “문장형 전세사기 검색어로 마감순/신규순까지 안정적으로 비교 가능하다”는 식의 데모 설명은 피하는 것이 좋다.
- 구현 상태:
  - 추천순/마감순/신규순 UI와 API 처리 흐름이 구현되어 있다.
  - HEAD 기준에서는 검색어 있는 날짜 정렬이 상위 10개 안에서만 이뤄지는 한계가 있다.
  - 로컬 데모 기준에서는 서버 재검색과 후보 확장으로 날짜 정렬 품질을 보완했다.

#### 데모 검색어 주의

- 문장형 전세사기 검색어는 추천순 데모용으로만 사용하는 것이 안전하다.
- 날짜 정렬 비교는 `서울 청년 창업 지원` 또는 서울+창업+25세 조합이 더 안정적이다.

### 3-4. AI 추천

- 실제 구현 흐름:
  - 마이페이지 또는 추천 화면에서 `/api/mypage/recommendations`를 호출한다.
  - API는 `user_info`에 저장된 프로필 조건을 조회한다.
  - `scraps`에 저장된 최근 스크랩 이력도 추천 후보 생성에 활용될 수 있다.
  - 프로필 추천과 스크랩 추천은 분리된 흐름으로 존재한다.
  - 항상 프로필과 스크랩을 둘 다 반영하는 것은 아니다.
  - 추천 후보는 조건 일치도와 유사도를 함께 보정해 화면에 표시된다.
- 관련 파일:
  - `web/app/api/mypage/recommendations/route.ts`
  - `web/app/page.tsx`
- 사용 DB/API:
  - Supabase `user_info`
  - Supabase `scraps`
  - Supabase `announcements`
  - Next.js API Route `/api/mypage/recommendations`
- 발표용 멘트:
  - “AI 추천은 로그인한 사용자의 저장된 프로필 조건이나 최근 스크랩 이력 중 사용할 수 있는 신호를 바탕으로 추천 후보를 찾는 기능입니다.”
  - “프로필이 충분히 저장되어 있으면 지역, 나이, 관심 분야 같은 프로필 조건으로 추천 query를 만들고, 최근 스크랩한 공고가 있으면 스크랩한 정책 제목과 요약을 바탕으로 비슷한 후보를 찾습니다.”
  - “중요한 점은 프로필과 스크랩을 항상 둘 다 반영한다고 말하면 안 된다는 것입니다. 사용자가 프로필을 입력했는지, 스크랩 이력이 있는지에 따라 프로필 기반 추천과 스크랩 기반 추천 흐름이 각각 동작할 수 있습니다.”
  - “가져온 후보는 그대로 보여주는 것이 아니라, 프로필 조건과의 일치도나 스크랩 이력에서 보이는 관심 분야, 그리고 vector similarity를 함께 참고해서 다시 정렬합니다.”
- 발표에서 말해도 되는 표현:
  - “저장된 프로필 정보나 최근 스크랩 이력을 바탕으로 추천 후보를 찾고, 조건 일치도와 유사도를 함께 보정해 보여줍니다.”
- 조심해야 할 표현:
  - “지역, 나이, 관심 분야, 스크랩 이력을 항상 모두 반영한다”는 표현은 피해야 한다.
  - “사용자 행동을 장기간 학습한 개인화 모델”처럼 말하면 안 된다.
- 구현 상태:
  - 프로필 기반 추천과 스크랩 기반 추천 API 흐름이 구현되어 있다.
  - 다만 화면 문구가 “스크랩 기반 추천”처럼 좁게 표현되어 있으면 실제 로직보다 과하게 단정할 수 있다.

### 3-5. 챗봇

- 실제 구현 흐름:
  - 사용자가 챗봇에 질문을 입력한다.
  - `/api/chat/rag`가 사용자 질문을 받는다.
  - API는 사용자 프로필 조건, 현재 대화 맥락, 질문 내용을 함께 고려한다.
  - 질문 안의 조건과 프로필 조건이 충돌할 수 있는지 확인한다.
  - 조건 충돌이나 프로필 조건 사용 여부에 따라 승인/거절 흐름을 거친다.
  - 검색 결과를 기반으로 Gemini가 답변을 생성한다.
  - 저장 대화는 `chat_history`에 transcript 형태로 저장된다.
- 관련 파일:
  - `web/app/api/chat/rag/route.ts`
  - 챗봇 UI 연결 파일
- 사용 DB/API:
  - Supabase `announcements`
  - Supabase `user_info`
  - Supabase `chat_history`
  - Gemini 답변 생성 API
  - Next.js API Route `/api/chat/rag`
- 발표용 멘트:
  - “챗봇은 사용자의 질문과 프로필 조건, 현재 대화 맥락을 바탕으로 검색용 query를 만들고, 같은 Supabase pgvector RPC 계열로 관련 공고를 먼저 찾습니다. 그 다음 검색된 공고 내용을 근거로 Gemini가 답변을 생성합니다. 저장 대화는 transcript로 남기지만, 과거 대화를 embedding으로 의미 검색하는 장기기억 기능까지 구현한 것은 아닙니다.”
- 발표에서 말해도 되는 표현:
  - “챗봇은 사용자 질문, 프로필 조건, 현재 대화 맥락을 함께 보고 관련 공고를 검색한 뒤 Gemini로 답변을 생성합니다.”
  - “프로필 조건과 질문 조건이 충돌할 수 있을 때는 사용자의 확인을 거치는 흐름을 두었습니다.”
- 조심해야 할 표현:
  - “저장된 과거 대화를 embedding으로 의미 검색해 장기 기억처럼 활용한다”는 표현은 피해야 한다.
  - `query_embedding` 기반 저장 대화 의미 검색은 미구현이다.
- 구현 상태:
  - RAG 기반 챗봇 흐름, 프로필 조건 고려, 조건 충돌 확인, Gemini 답변 생성, transcript 저장이 구현되어 있다.
  - 저장 대화의 의미 검색은 아직 구현된 기능으로 말하면 안 된다.

### 3-6. 스크랩/마이페이지/프로필

- 실제 구현 흐름:
  - 사용자가 공고 카드에서 스크랩 버튼을 누르면 스크랩 토글이 실행된다.
  - `/api/mypage/scraps`가 스크랩 저장, 삭제, 조회를 처리한다.
  - `scraps` 테이블에 사용자별 관심 공고가 저장된다.
  - 저장된 스크랩은 AI 추천 후보를 만들 때 재사용될 수 있다.
  - 프로필 정보는 `/api/mypage/info`를 통해 저장/조회된다.
- 관련 파일:
  - `web/app/page.tsx`
  - `web/app/api/mypage/scraps/route.ts`
  - `web/app/api/mypage/info/route.ts`
  - `web/app/api/mypage/recommendations/route.ts`
- 사용 DB/API:
  - Supabase `scraps`
  - Supabase `user_info`
  - Next.js API Route `/api/mypage/scraps`
  - Next.js API Route `/api/mypage/info`
- 발표용 멘트:
  - “스크랩은 사용자가 관심 있는 공고를 저장해두고 나중에 다시 볼 수 있게 하는 기능입니다. 공고 카드에서 스크랩 버튼을 누르면 `/api/mypage/scraps`를 통해 사용자별 스크랩 데이터가 저장되거나 삭제됩니다.”
  - “이 스크랩은 단순 저장 목록으로만 끝나지 않고, 이후 추천 후보를 만들 때 사용자의 관심 이력 중 하나로 다시 활용될 수 있습니다.”
  - “프로필은 별도의 `/api/mypage/info` API로 저장됩니다. 지역, 나이, 관심 분야 같은 값을 저장해두면 AI 추천이나 챗봇에서 사용 가능한 조건으로 참고할 수 있습니다.”
  - “다만 스크랩은 신청이나 접수 상태가 아닙니다. 발표에서는 ‘관심 공고 저장’과 ‘추천에 참고될 수 있는 관심 이력’ 정도로만 설명하는 것이 정확합니다.”
- 발표에서 말해도 되는 표현:
  - “사용자가 저장한 스크랩은 마이페이지에서 다시 볼 수 있고, 추천 후보를 보정하는 데도 활용됩니다.”
  - “프로필 정보는 지역, 나이, 관심 분야 같은 사용자 조건을 저장하는 데 사용됩니다.”
- 조심해야 할 표현:
  - “신청 현황 관리가 구현되어 있다”는 표현은 피해야 한다.
  - 스크랩을 신청이나 접수 데이터처럼 설명하면 안 된다.
- 구현 상태:
  - 스크랩 토글, 저장, 삭제, 조회, 프로필 저장/조회가 구현되어 있다.
  - 신청 현황은 구현된 기능처럼 말하지 않아야 한다.

### 3-7. 공고 카드/더보기

- 실제 구현 흐름:
  - 검색/추천 API가 공고 리스트를 반환한다.
  - 기본 카드에는 제목, 요약, 기관, 분야, 지역, 마감 정보 등이 표시된다.
  - 더보기를 누르면 단순 UI 확장이 아니라 DB 상세 필드가 추가로 노출된다.
  - 지원 내용, 신청 방법, 제출 서류, 추가 조건, 연령 조건, 대상, 상세 URL 등 발표에서 보여주기 좋은 실제 데이터가 연결되어 있다.
- 관련 파일:
  - `web/app/page.tsx`
  - `web/app/api/search/route.ts`
  - Supabase `announcements`
- 사용 DB/API:
  - Supabase `announcements`
  - Next.js API Route `/api/search`
- 발표용 멘트:
  - “공고 카드는 검색 결과를 간단히 보여주는 요약 UI이고, 더보기를 누르면 DB에 저장된 상세 필드를 펼쳐서 볼 수 있게 했습니다.”
  - “예를 들어 제목, 기관, 지역, 분야 같은 기본 정보뿐 아니라 지원 내용, 신청 방법, 제출 서류, 추가 조건, 연령 조건, 상세 URL 같은 필드를 연결했습니다.”
  - “이 부분은 단순히 화면을 길게 펼치는 UI가 아니라, 수집하고 정규화한 정책 데이터가 실제로 서비스 화면까지 이어졌다는 것을 보여주는 데모 포인트입니다.”
  - “다만 원본 API마다 제공하는 상세도는 다르기 때문에 모든 공고의 모든 상세 필드가 항상 완벽하게 채워져 있다고 말하지는 않는 게 좋습니다.”
- 발표에서 말해도 되는 표현:
  - “공고 카드는 검색 결과 요약만 보여주는 것이 아니라, 더보기에서 DB에 저장된 상세 신청 정보를 확인할 수 있게 연결했습니다.”
- 조심해야 할 표현:
  - “모든 공고의 상세 필드가 항상 완전하게 채워져 있다”는 표현은 피해야 한다.
- 구현 상태:
  - 카드 기본 정보와 더보기 상세 필드 연결이 구현되어 있다.

| 화면 항목 | 연결 DB 컬럼/필드 | 데이터 출처 | 구현 상태 | 비고 |
|---|---|---|---|---|
| 제목 | `title` | 온통청년/기업마당/K-Startup 정규화 결과 | 구현됨 | 카드의 대표 표시 항목 |
| 요약 | `summary` 또는 설명 계열 필드 | 원본 설명 텍스트 정규화 | 구현됨 | 공고별 품질 차이가 있을 수 있음 |
| 기관 | `organization` 또는 기관명 필드 | 원본 제공 기관 정보 | 구현됨 | 출처별 명칭 포맷이 다를 수 있음 |
| 분야 | `category`, `s_category` 계열 | 정규화/분류 결과 | 구현됨 | 조건검색 hard filter에 활용 |
| 지역 | `region` | 원본 지역 정보 정규화 | 구현됨 | 지역 exact match 보정에도 활용 |
| 지원 내용 | `support_content` 계열 상세 필드 | 원본 상세 설명 | 구현됨 | 더보기에서 발표하기 좋은 항목 |
| 신청 방법 | `apply_method` 계열 | 원본 신청 안내 | 구현됨 | 비어 있는 공고가 있을 수 있음 |
| 제출 서류 | `required_documents` 계열 | 원본 제출 안내 | 구현됨 | 출처별 상세도 차이 있음 |
| 추가 조건 | `additional_conditions` 계열 | 원본 조건 텍스트 | 구현됨 | 정규화 품질 확인 필요 |
| 연령 조건 | `age_min`, `age_max`, 연령 텍스트 계열 | 원본 대상 조건 정규화 | 구현됨 | 나이 보정에 사용 |
| 대상 | `target` 또는 대상 설명 필드 | 원본 대상 조건 | 구현됨 | 대부분 hard filter로 단정하면 안 됨 |
| 대상 태그 | 대상/연령/분야 파생 태그 | 정규화 또는 UI 가공 | 구현됨 | UI 표시용 성격 |
| 상세 URL | `detail_url`, `url` 계열 | 원본 공고 링크 | 구현됨 | 실제 신청은 외부 상세 페이지로 이동 |

### 3-8. 관리자 대시보드

- 실제 구현 흐름:
  - 관리자 화면은 DB 상태를 읽어서 보여주는 read-only 관리 화면이다.
  - 공고 수, 상태별 공고 수, 가입자 수, 스크랩 수, 분야/지역/source 분포, 인기 스크랩, 최근 가입자 등을 조회한다.
  - 정책 추가/수정/삭제 같은 CRUD 관리 기능은 구현된 것으로 말하면 안 된다.
- 관련 파일:
  - 관리자 화면 관련 API/UI 파일
  - Supabase `announcements`
  - Supabase `user_info`
  - Supabase `scraps`
- 사용 DB/API:
  - Supabase `announcements`
  - Supabase `user_info`
  - Supabase `scraps`
  - 필요 시 batch log 조회 API
- 실제 DB 기반 항목:
  - 공고 수
  - 모집중/마감/마감임박/상시
  - 가입자 수
  - 스크랩 수
  - 분야/지역/source 분포
  - 인기 스크랩 TOP 10
  - 최근 가입자
- 구현된 것처럼 말하면 안 되는 항목:
  - 정책 추가/수정/삭제
  - 조회수
  - 신청건수
  - 전환율
  - MAU
  - 최근 활동 로그
- 발표용 멘트:
  - “관리자 대시보드는 운영자가 데이터를 수정하는 CRUD 화면이라기보다, 현재 DB에 쌓여 있는 공고와 사용자 조건, 스크랩 집계를 한눈에 확인하는 read-only 화면입니다.”
  - “공고가 총 몇 개인지, 모집중/마감/마감임박/상시 상태가 어떻게 나뉘는지, 사용자 프로필이나 관심 분야가 어떤 분포를 보이는지, 어떤 공고가 많이 스크랩됐는지를 확인할 수 있습니다.”
  - “이 화면은 mock 통계가 아니라 Supabase에 저장된 실제 테이블을 기준으로 집계한다는 점이 포인트입니다.”
  - “다만 여기서 정책을 추가하거나 수정하거나 삭제하는 기능, 조회수나 신청건수, 전환율, MAU 같은 운영 분석 지표까지 구현된 것은 아니기 때문에 발표에서는 read-only 통계 조회 화면이라고 설명해야 합니다.”
- 발표에서 사용 가능한 문구:
  - “관리자 대시보드는 실제 DB에 저장된 공고, 사용자 프로필 분포, 관심 분야 분포, 스크랩 집계를 확인하는 read-only 관리 화면입니다.”
- 구현 상태:
  - read-only 통계 조회 화면으로 설명하는 것이 정확하다.
  - 운영 관리자 CMS나 분석 대시보드처럼 과하게 설명하면 안 된다.

## 4. 화면 문구 검증 결과

| 화면/위치 | 현재 문구 | 실제 로직과 일치 여부 | 문제점 | 추천 문구 | 우선순위 |
|---|---|---|---|---|---|
| 통합검색 추천순 tooltip | “검색어 의미와 검색어에서 읽어낸 지역/분야/나이 조건을 반영한 추천 순서예요.” | 일부 불일치 | 분야는 self-query로 추출되어도 hard filter/점수에 직접 반영되지 않는다. | “검색어 유사도와 적용된 필터 조건을 함께 참고해 관련 공고를 먼저 보여드려요.” | P0 |
| 조건검색 추천순 tooltip | 조건검색에서도 의미 기반 추천처럼 보이는 설명 | 불일치 가능 | 조건검색은 embedding 의미 검색이 아니라 table filter + condition_score 중심이다. | “선택한 필터 조건에 맞는 공고를 조건 적합도순으로 보여드려요.” | P0 |
| 스크랩 페이지 추천 섹션 | “스크랩 기반 추천 공고” | 일부 불일치 | 같은 API가 프로필 기반 추천도 섞을 수 있다. | “맞춤 추천 공고” | P0 |
| 스크랩 추천 카드 상태 | “진행중” | 불일치 가능 | 추천 카드 상태가 실제 공고 상태가 아니라 hardcode일 가능성이 있다. | 실제 상태 표시 또는 문구 제거 | P0 |
| AI 추천 카드 설명 | “지역, 나이, 관심 분야와 스크랩 이력 기반” | 일부 불일치 | 항상 모든 조건과 스크랩 이력을 함께 쓰는 것은 아니다. | “저장한 프로필 또는 스크랩 이력을 바탕으로 추천합니다.” | P1 |
| 관리자 헤더 | “수집 로그” | 일부 불일치 | API는 batchLogs를 조회하지만 화면에서는 수집 로그 영역이 숨김 처리되어 있다. | “공고, 사용자 조건, 스크랩 집계를 조회 전용으로 확인합니다.” | P1 |
| 대상 필터 설명 | 대상 필터가 직접 필터처럼 보이는 설명 | 불일치 | 대상 필터는 대부분 실제 hard filter가 아니다. | “대상 중 청년/시니어는 나이 조건으로 보조 반영됩니다.” | P0 |

## 5. 발표에서 조심해야 할 표현

| 기능 | 피해야 할 표현 | 추천 표현 | 이유 |
|---|---|---|---|
| 대상 필터 | “대상 필터로 청년/기업/소상공인/여성 등을 정확히 걸러냅니다.” | “대상 조건은 UI에서 선택할 수 있고, 청년/시니어는 나이 조건으로 보조 반영됩니다.” | 대상 필터는 대부분 hard filter가 아니다. |
| 통합검색 추천순 | “검색어에서 지역, 분야, 나이를 추출해 모두 점수에 반영합니다.” | “검색어 유사도와 일부 조건 보정을 함께 사용합니다.” | 분야는 통합검색 final_score에 직접 가점으로 들어가지 않는다. |
| 조건검색 추천순 | “조건검색도 AI 의미 검색으로 정렬합니다.” | “조건검색은 선택한 필터 조건의 적합도 중심으로 정렬합니다.” | 조건검색은 table filter + condition_score 중심이다. |
| AI 추천 | “프로필과 스크랩을 항상 모두 반영합니다.” | “저장된 프로필 정보나 최근 스크랩 이력을 바탕으로 추천 후보를 찾습니다.” | 프로필 추천과 스크랩 추천은 분리되어 있고 항상 둘 다 쓰는 것은 아니다. |
| 관리자 대시보드 | “관리자가 정책을 추가, 수정, 삭제할 수 있습니다.” | “실제 DB 집계를 확인하는 read-only 관리 화면입니다.” | CRUD 기능은 구현된 기능처럼 말하면 안 된다. |
| 저장 대화/query_embedding | “과거 대화를 embedding으로 검색해 장기 기억처럼 활용합니다.” | “대화 transcript를 저장하며, 현재 대화 맥락과 프로필 조건을 답변에 활용합니다.” | `query_embedding` 기반 저장 대화 의미 검색은 미구현이다. |
| 신청/조회수/전환율/알림 | “신청 현황, 조회수, 전환율, 알림까지 제공합니다.” | “현재 데모는 검색, 추천, 챗봇, 스크랩, 프로필, read-only 통계 중심입니다.” | 신청건수, 조회수, 전환율, MAU, 알림은 구현된 기능처럼 보이면 안 된다. |
| 자동 수집/자동 업데이트 | “정책 데이터가 자동으로 매일 수집되고 업데이트됩니다.” | “수집·정제·업로드 파이프라인을 구성했고, 현재는 수동 실행 성격이 있습니다.” | 완전 자동 배치와 로그 기록까지 확정된 상태가 아니다. |

## 6. 통합검색과 조건검색을 따로 둔 이유

### UX 관점

- 통합검색은 사용자가 자연어/키워드로 바로 찾는 흐름이다.
  - 예: `서울 청년 창업 지원`
  - 예: `전세사기 피해 청년 지원`
- 조건검색은 지역, 분야, 나이처럼 명확한 조건을 직접 선택해 후보를 좁히는 흐름이다.
  - 예: 지역 `서울`, 분야 `창업`, 나이 `25세`
- 두 검색은 같은 정책 공고를 찾지만 사용자의 출발점이 다르다.
  - 통합검색: “말로 설명하기”
  - 조건검색: “조건을 직접 선택하기”

### 구현 관점

- 둘 다 `/api/search`를 공유한다.
- 하지만 모드에 따라 검색 경로와 정렬 방식이 달라진다.
  - 통합검색은 검색어 embedding similarity를 중심으로 한다.
  - 조건검색은 DB 필터와 condition_score를 중심으로 한다.
- 이렇게 나누면 자연어 검색과 명시적 필터 검색을 한 화면에서 제공하면서도, 각 검색 방식의 장점을 살릴 수 있다.

### 발표용 설명 멘트

> “통합검색은 말로 설명하기, 조건검색은 조건을 직접 선택하기에 맞춘 UX입니다. 내부적으로는 둘 다 같은 `/api/search`를 사용하지만, 통합검색은 검색어 의미 유사도를 중심으로 보고, 조건검색은 사용자가 고른 지역, 분야, 나이 조건의 적합도를 중심으로 후보를 정렬합니다.”

## 7. 잘 구현된 점

### Python 수집/정제 → Supabase → Next.js API → 화면까지 흐름 연결

- 무엇이 잘 구현됐는지:
  - 외부 정책 API에서 데이터를 가져와 정규화하고, Supabase에 저장한 뒤, Next.js API Route를 통해 웹 화면에서 검색/추천으로 보여주는 end-to-end 흐름이 연결되어 있다.
- 왜 잘 됐는지:
  - 수집, 정규화, DB 저장, API, 화면 표시 역할이 비교적 명확하게 나뉘어 있다.
- 어떤 구조 덕분인지:
  - `app/collectors/*.py`, `app/norm.py`, notebook, Supabase `announcements`, Next.js API Route가 단계별 책임을 가진 구조다.
- 관련 파일:
  - `scripts/fetch.py`
  - `app/collectors/biz.py`
  - `app/collectors/kst.py`
  - `app/collectors/youth.py`
  - `app/norm.py`
  - `policyRec_v1_2_2.ipynb`
  - `policyRec_v1_2_3.ipynb`
  - `web/app/api/search/route.ts`
  - `web/app/page.tsx`
- 발표에서 말할 포인트:
  - “단순 화면 mock이 아니라, 실제 수집·정제된 정책 데이터가 DB와 API를 거쳐 화면까지 이어지는 구조를 만들었습니다.”

### Hybrid RPC 실패 시 fallback

- 무엇이 잘 구현됐는지:
  - Supabase RPC 기반 의미 검색이 실패하거나 결과가 부족할 때 fallback 검색을 수행한다.
- 왜 잘 됐는지:
  - 외부 API나 DB 함수에 문제가 있어도 데모와 서비스 흐름이 완전히 끊기지 않는다.
- 어떤 구조 덕분인지:
  - `/api/search` 안에서 RPC 결과와 fallback 결과를 분기 처리하는 구조다.
- 관련 파일:
  - `web/app/api/search/route.ts`
  - `Database/RPC_match_announcements_hybrid_v1.sql`
- 발표에서 말할 포인트:
  - “검색 품질뿐 아니라 장애 상황을 고려해 fallback 경로를 둔 점이 구현상 안정성을 높였습니다.”

### 공고 카드 더보기와 DB 상세 컬럼 연결

- 무엇이 잘 구현됐는지:
  - 공고 카드의 더보기 영역이 단순 텍스트 확장이 아니라 DB 상세 필드와 연결되어 있다.
- 왜 잘 됐는지:
  - 사용자가 검색 결과에서 바로 지원 내용, 신청 방법, 제출 서류, 상세 URL을 확인할 수 있다.
- 어떤 구조 덕분인지:
  - `announcements` 테이블에 상세 필드를 저장하고, API 응답에서 카드 UI로 전달하는 구조다.
- 관련 파일:
  - `web/app/page.tsx`
  - `web/app/api/search/route.ts`
  - `Database/supabase_table_create_query.txt`
- 발표에서 말할 포인트:
  - “검색 결과 카드에서 실제 신청에 필요한 상세 정보까지 확인할 수 있도록 DB 컬럼과 UI를 연결했습니다.”

### 관리자 화면을 mock이 아니라 read-only DB 지표 중심으로 정리

- 무엇이 잘 구현됐는지:
  - 관리자 화면이 가짜 지표가 아니라 실제 DB 기반 공고 수, 사용자 수, 스크랩 수, 분야/지역/source 분포를 보여주는 방향으로 정리되어 있다.
- 왜 잘 됐는지:
  - 서비스 운영자가 확인할 수 있는 최소한의 데이터 현황을 실제 DB 기준으로 볼 수 있다.
- 어떤 구조 덕분인지:
  - `announcements`, `user_info`, `scraps` 등 실제 테이블을 조회하는 read-only 구조다.
- 관련 파일:
  - 관리자 API/UI 관련 파일
  - Supabase 테이블 정의
- 발표에서 말할 포인트:
  - “관리자 화면은 정책 CRUD가 아니라, 현재 DB에 들어온 공고와 사용자 관심 데이터를 조회하는 read-only 대시보드입니다.”

### 챗봇의 프로필/질문/대화 맥락 우선순위와 충돌 확인 흐름

- 무엇이 잘 구현됐는지:
  - 챗봇이 사용자 질문만 보는 것이 아니라 프로필 조건과 현재 대화 맥락도 함께 고려한다.
  - 질문 조건과 프로필 조건이 충돌할 수 있을 때 승인/거절 흐름을 둔다.
- 왜 잘 됐는지:
  - 정책 추천 챗봇에서 중요한 “내 조건 기준으로 찾을지, 이번 질문 기준으로 찾을지” 문제를 UX 흐름으로 다뤘다.
- 어떤 구조 덕분인지:
  - `/api/chat/rag`에서 질문, 프로필, 대화 맥락, 검색 결과를 조합하는 구조다.
- 관련 파일:
  - `web/app/api/chat/rag/route.ts`
  - Supabase `user_info`
  - Supabase `chat_history`
- 발표에서 말할 포인트:
  - “챗봇은 단순 Q&A가 아니라, 사용자의 저장 조건과 현재 질문 조건이 충돌할 수 있다는 점을 고려해 확인 흐름을 넣었습니다.”

## 8. 아쉬웠던 점 / 제대로 안 됐던 점

### 대상 필터가 대부분 hard filter가 아닌 점

- 무엇이 부족했는지:
  - 대상 필터가 UI에는 존재하지만, 대부분의 대상 유형이 DB hard filter로 정확히 적용되는 구조는 아니다.
- 왜 어려웠는지:
  - 원본 데이터의 대상 조건이 자유 텍스트이거나 출처별 표현이 달라 정규화가 어렵다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - “대상 중 청년/시니어는 나이 조건으로 보조 반영됩니다”라고 설명한다.
  - 대상 필터를 핵심 데모 포인트로 길게 보여주지 않는다.
- 향후 개선 방향:
  - 대상 조건을 표준 태그로 정규화하고, 대상별 hard filter 컬럼을 추가한다.

### 통합검색 self-query category는 추출되지만 hard filter/가점으로 직접 쓰지 않는 점

- 무엇이 부족했는지:
  - 검색어에서 분야가 추출될 수 있어도 통합검색 final_score에 직접 반영되지 않는다.
- 왜 어려웠는지:
  - 자연어에서 뽑은 분야를 hard filter로 쓰면 관련 공고를 과하게 누락할 수 있다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - “검색어 유사도와 일부 조건 보정”이라고 표현한다.
  - “분야까지 정확히 점수화한다”는 표현을 피한다.
- 향후 개선 방향:
  - self-query category를 soft boost로 반영하되, 후보 누락을 막는 threshold를 함께 설계한다.

### HEAD 기준 검색어 있는 마감순/신규순의 한계

- 무엇이 부족했는지:
  - HEAD 기준에서는 검색어가 있는 마감순/신규순이 이미 받아온 상위 10개 결과 안에서 날짜 정렬된다.
- 왜 어려웠는지:
  - 의미 검색 후보를 넓게 가져온 뒤 날짜 정렬하는 서버 로직이 충분히 보강되지 않은 상태였다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - GitHub HEAD 기준 발표에서는 날짜 정렬 품질 한계를 인정한다.
  - 로컬 데모 기준에서는 서버에 `sort_by`를 보내고 후보 50개 중 관련도 floor 후 날짜 정렬하도록 보완한 상태라고 구분한다.
  - 날짜 정렬 비교는 `서울 청년 창업 지원` 또는 서울+창업+25세 조합을 사용한다.
- 향후 개선 방향:
  - 검색어가 있는 모든 정렬 모드에서 서버가 충분한 후보를 가져온 뒤 정렬하도록 통일한다.

### 저장 대화는 있지만 `query_embedding` 기반 저장 대화 검색은 아직 없는 점

- 무엇이 부족했는지:
  - `chat_history`에 transcript 저장은 가능하지만, 저장 대화를 embedding으로 검색해 답변에 활용하는 구조는 아직 없다.
- 왜 어려웠는지:
  - 대화 저장, 개인정보, 검색 품질, 비용을 함께 고려해야 한다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - “대화 transcript를 저장합니다”까지만 말한다.
  - “장기 기억”이나 “과거 대화 의미 검색”이라고 말하지 않는다.
- 향후 개선 방향:
  - 대화 요약 embedding을 별도로 저장하고, 사용자의 동의를 받은 범위에서 의미 검색을 붙인다.

### 미구현 기능이 구현된 것처럼 보일 수 있는 점

- 무엇이 부족했는지:
  - 알림, 신청현황, 조회수, 신청건수, 전환율, MAU 같은 항목은 구현된 핵심 기능이 아니다.
- 왜 어려웠는지:
  - 실제 운영 지표를 만들려면 이벤트 로깅, 신청 추적, 알림 발송, 사용자 행동 수집이 필요하다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - 검색, 추천, 챗봇, 스크랩, 프로필, read-only 통계 중심으로 소개한다.
  - 신청/조회수/전환율/알림은 향후 개선 방향으로만 언급한다.
- 향후 개선 방향:
  - 이벤트 로그 테이블과 사용자 액션 추적을 설계한 뒤 관리자 통계와 연결한다.

### 스크랩 페이지 문구와 추천 카드 상태 문제

- 무엇이 부족했는지:
  - “스크랩 기반 추천” 문구는 프로필 추천이 섞일 수 있는 실제 API와 어긋날 수 있다.
  - 추천 카드의 “진행중” 상태는 실제 공고 상태가 아니라 hardcode일 가능성이 있다.
- 왜 어려웠는지:
  - 추천 API가 여러 추천 근거를 다루는데, 화면 문구가 하나의 근거로만 좁혀 표현되어 있다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - 문구 수정 전이라면 “스크랩/프로필 기반 추천이 섞일 수 있다”고 설명한다.
  - 추천 카드 상태는 길게 설명하지 않거나 실제 상태 표시 여부를 확인한다.
- 향후 개선 방향:
  - 섹션명을 “맞춤 추천 공고”로 바꾸고, 카드 상태는 실제 DB 상태로 표시한다.

### 관리자 대시보드는 CRUD가 아니라 read-only인 점

- 무엇이 부족했는지:
  - 관리자 화면은 정책 추가/수정/삭제 기능이 아니라 조회 전용 통계 화면이다.
- 왜 어려웠는지:
  - CRUD를 만들려면 권한, 검수, 데이터 품질, 원본 출처와의 동기화 정책이 필요하다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - “read-only 관리 화면”이라고 명확히 설명한다.
- 향후 개선 방향:
  - 관리자 권한 체계와 검수 workflow를 설계한 뒤 CRUD를 추가한다.

### 로컬 버전과 GitHub HEAD 기준이 섞이면 데모 기준이 흔들리는 점

- 무엇이 부족했는지:
  - GitHub HEAD 기준과 로컬 데모 기준의 검색 정렬 동작이 다르다.
- 왜 어려웠는지:
  - 데모 품질 보완 수정이 아직 HEAD에 반영되지 않은 상태이기 때문이다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - 발표 문서와 데모 스크립트에서 “HEAD 기준”과 “로컬 데모 기준”을 분리한다.
- 향후 개선 방향:
  - 로컬 변경을 검증한 뒤 커밋하거나, 발표 기준을 하나로 고정한다.

### GPU 자원을 충분히 활용하지 못한 점

- 무엇이 부족했는지:
  - GPU 지원이 가능했지만, 이번 프로젝트는 직접 모델을 학습하기보다는 정책 데이터를 수집·정제하고 Supabase DB와 Next.js 웹을 통해 검색, 추천, 챗봇 기능을 연결하는 서비스형 프로젝트에 가까웠다.
- 왜 어려웠는지:
  - 자체 임베딩 생성, 공고 분류 모델 학습, 검색 결과 리랭킹 모델 실험도 고려했지만, 프로젝트 기간 안에서는 전체 서비스 흐름을 완성하고 안정적으로 시연 가능한 상태로 만드는 것을 우선했다.
- 데모에서는 어떻게 설명하거나 우회할지:
  - GPU를 쓰지 못했다는 방어적 표현보다, “서비스 흐름 완성에 우선순위를 두었다”고 설명한다.
- 향후 개선 방향:
  - 자체 임베딩 모델 실험, 공고 카테고리 분류 모델, 검색 결과 리랭킹 모델을 GPU 기반으로 실험한다.
- 회고 문장:
  - GPU 지원이 가능했지만, 이번 프로젝트는 직접 모델을 학습하기보다는 정책 데이터를 수집·정제하고 Supabase DB와 Next.js 웹을 통해 검색, 추천, 챗봇 기능을 연결하는 서비스형 프로젝트에 가까웠다. 자체 임베딩 생성, 공고 분류 모델 학습, 검색 결과 리랭킹 모델 실험도 고려했지만, 프로젝트 기간 안에서는 전체 서비스 흐름을 완성하고 안정적으로 시연 가능한 상태로 만드는 것을 우선했다. 결과적으로 GPU 지원 자원을 충분히 활용하지 못한 점은 아쉬움으로 남았다.

## 9. 20분 발표자료에 반영할 핵심 내용

| 발표 파트 | 시간 | 핵심 메시지 | 연결되는 데모/로직 |
|---|---:|---|---|
| 1. 프로젝트 배경 | 1분 30초 | 정책 정보는 많지만 사용자 조건에 맞는 공고를 찾기 어렵다. | 문제 상황 소개 |
| 2. 문제 정의 | 1분 30초 | 사용자가 검색어, 조건, 프로필, 스크랩을 통해 맞춤형 정책 공고를 찾게 하는 것이 목표다. | 서비스 목표 |
| 3. 전체 아키텍처 | 2분 | 외부 API → Python 정규화 → Supabase → Next.js API → 화면으로 이어지는 구조다. | 전체 시스템 흐름도 |
| 4. 데이터 수집/정제 흐름 | 2분 | 온통청년, 기업마당, K-Startup 데이터를 공통 스키마로 정규화했다. | collector, `app/norm.py`, notebook |
| 5. DB 구조 | 2분 | `announcements` 중심으로 공고 상세, 사용자 프로필, 스크랩, 대화 이력을 저장한다. | Supabase 테이블 설명 |
| 6. 검색/추천/챗봇 로직 | 4분 | 통합검색은 embedding similarity, 조건검색은 조건 적합도, 챗봇은 RAG 흐름을 사용한다. | `/api/search`, `/api/mypage/recommendations`, `/api/chat/rag` |
| 7. 주요 화면 데모 | 4분 | 검색, 조건검색, 더보기, 스크랩, AI 추천, 챗봇, 관리자 화면을 짧게 보여준다. | 실제 웹 화면 |
| 8. 구현하면서 어려웠던 점 | 1분 30초 | 데이터 품질, 필터 정합성, 정렬 품질, 문구와 실제 로직 일치가 어려웠다. | 한계 설명 |
| 9. 한계와 개선 방향 | 1분 30초 | 자동화, 대상 필터 고도화, 대화 의미 검색, 운영 지표, GPU 기반 모델 실험이 다음 과제다. | 회고 및 개선 |

## 10. 발표 멘트 초안

### 프로젝트 배경

“정책 지원사업 정보는 여러 기관에 흩어져 있고, 사용자가 자기 조건에 맞는 공고를 찾으려면 지역, 나이, 분야, 신청 기간을 계속 비교해야 합니다. PolicyRec은 이런 불편을 줄이기 위해 정책 공고 데이터를 수집하고, 사용자의 검색어와 조건에 맞춰 관련 공고를 추천하는 서비스로 기획했습니다.”

### 전체 구현 흐름

“전체 구조는 외부 정책 API에서 데이터를 가져오는 것으로 시작합니다. 온통청년, 기업마당, K-Startup 데이터를 Python 코드로 수집하고, 서로 다른 필드를 공통 스키마로 정규화한 뒤 Supabase에 저장했습니다. 웹에서는 Next.js API Route가 Supabase와 Gemini API를 호출하고, 화면은 그 결과를 검색, 추천, 챗봇, 스크랩, 관리자 기능으로 보여줍니다.”

### 데이터 수집/정제

“수집 단계에서는 각 API의 응답 구조가 다르기 때문에 collector를 분리했습니다. 이후 `app/norm.py`에서 제목, 기관, 지역, 분야, 신청 기간, 대상, 지원 내용 같은 필드를 공통 구조로 맞췄습니다. notebook은 정규화 결과를 검수하고 embedding 생성과 Supabase 업로드 흐름을 확인하는 데 사용했습니다.”

### Supabase DB

“Supabase에서는 `announcements` 테이블이 핵심입니다. 정규화된 공고 정보와 상세 신청 정보, embedding을 저장하고, 사용자 프로필은 `user_info`, 스크랩은 `scraps`, 챗봇 대화는 `chat_history`에 저장하는 방식으로 구성했습니다.”

### Next.js API Route

“화면이 DB를 직접 조회하지 않고 Next.js API Route를 거치게 했습니다. `/api/search`는 통합검색과 조건검색을 처리하고, `/api/mypage/recommendations`는 맞춤 추천, `/api/chat/rag`는 챗봇 답변 생성을 담당합니다. 이렇게 화면과 데이터 로직을 분리해 유지보수하기 쉽게 만들었습니다.”

### 통합검색

“통합검색은 사용자가 자연어 또는 키워드로 입력한 검색어를 Gemini embedding으로 바꾸고, Supabase RPC에서 공고 embedding과의 유사도를 계산합니다. 여기서 지역이나 나이 보정이 들어간다는 것은 로그인한 사용자의 프로필을 항상 자동 반영한다는 뜻이 아니라, 화면에서 지역/나이 조건이 같이 전달됐거나 검색어 안에서 명확한 지역/나이 조건을 읽어낸 경우에 보조 점수로 활용한다는 의미입니다. 그리고 주 검색 경로인 `match_announcements_hybrid` RPC가 실패하면 기존 `match_announcements` RPC로 id와 similarity를 가져오고, 다시 `announcements` 테이블에서 상세 컬럼을 보강하는 fallback 흐름을 둬서 검색이 바로 끊기지 않게 했습니다.”

### 조건검색

“조건검색은 사용자가 지역, 분야, 나이 같은 조건을 직접 선택하는 흐름입니다. 지역과 분야는 DB 필터 성격으로 적용하고, 나이는 공고의 연령 조건과 비교해 조건 적합도를 보정합니다. 대상 필터는 현재 대부분 hard filter는 아니기 때문에, 청년이나 시니어처럼 나이 조건으로 해석 가능한 부분을 보조적으로 반영한다고 보는 것이 정확합니다.”

### 통합검색과 조건검색을 나눈 이유

“통합검색은 말로 설명하기, 조건검색은 조건을 직접 선택하기에 맞춘 UX입니다. 내부적으로는 둘 다 `/api/search`를 공유하지만, 통합검색은 검색어 의미 유사도를 중심으로 보고, 조건검색은 선택한 조건의 적합도를 중심으로 후보를 좁힙니다.”

### 추천순 정렬

“추천순은 모드에 따라 기준이 다릅니다. 통합검색에서는 검색어와 공고가 의미적으로 얼마나 가까운지가 중심이고, 화면 조건이나 검색어에서 읽어낸 지역/나이 조건이 있을 때만 보조 점수를 더합니다. 조건검색에서는 검색어 의미 유사도보다 사용자가 직접 고른 조건과 얼마나 잘 맞는지가 중심입니다. 내부적으로는 지역, 분야, 명시적으로 입력한 나이에 condition_score를 주고, 점수가 같으면 마감일이 빠른 공고를 먼저 보여줍니다.”

### AI 추천

“AI 추천은 로그인한 사용자의 저장된 프로필 정보나 최근 스크랩 이력 중 사용할 수 있는 신호를 바탕으로 추천 후보를 찾습니다. 프로필이 충분히 있으면 지역, 나이, 관심 분야로 추천 query를 만들고, 스크랩 이력이 있으면 최근 저장한 공고 제목과 요약을 바탕으로 비슷한 후보를 찾습니다. 다만 항상 프로필과 스크랩을 모두 반영하는 것은 아니어서, 발표에서는 ‘저장된 프로필 조건 또는 스크랩 이력을 바탕으로 추천한다’고 설명하는 것이 정확합니다.”

### 챗봇

“챗봇은 사용자의 질문, 저장된 프로필 조건, 현재 대화 맥락을 함께 고려합니다. 질문 속 조건과 프로필 조건이 충돌할 수 있을 때는 확인 흐름을 거치고, 검색 결과를 기반으로 Gemini가 답변을 생성합니다. 대화 내용은 transcript 형태로 저장되지만, 아직 저장 대화를 embedding으로 의미 검색하는 장기 기억 기능은 구현하지 않았습니다.”

### 공고 더보기

“공고 카드의 더보기는 단순히 UI를 펼치는 기능이 아니라 DB 상세 필드와 연결되어 있습니다. 지원 내용, 신청 방법, 제출 서류, 추가 조건, 연령 조건, 대상, 상세 URL 같은 정보를 공고별로 확인할 수 있습니다.”

### 스크랩/마이페이지

“사용자는 관심 있는 공고를 스크랩할 수 있고, 스크랩한 공고는 마이페이지에서 다시 볼 수 있습니다. 이 스크랩은 신청이나 접수 상태가 아니라 관심 공고 저장 기능입니다. 저장된 스크랩 이력은 이후 맞춤 추천에서 사용자의 관심 신호 중 하나로 재사용될 수 있고, 프로필 정보는 `/api/mypage/info`를 통해 별도로 저장해서 추천이나 챗봇이 참고할 수 있는 조건으로 활용합니다.”

### 관리자 대시보드

“관리자 대시보드는 실제 DB에 저장된 공고, 사용자 프로필 분포, 관심 분야 분포, 스크랩 집계를 확인하는 read-only 관리 화면입니다. 정책 추가, 수정, 삭제를 하는 CMS가 아니라 현재 서비스 데이터 상태를 확인하는 통계 화면에 가깝습니다. 그래서 조회수, 신청건수, 전환율, MAU 같은 운영 분석 지표까지 구현했다고 말하기보다는, 공고와 사용자 조건, 스크랩 집계를 조회 전용으로 확인한다고 설명하는 것이 정확합니다.”

### 구현하면서 어려웠던 점

“가장 어려웠던 부분은 데이터 출처마다 필드 구조와 표현 방식이 다르다는 점이었습니다. 특히 대상 조건은 자유 텍스트로 들어오는 경우가 많아서 모든 대상을 정확한 hard filter로 만들기 어려웠습니다. 또 추천순, 마감순, 신규순이 실제 사용자가 기대하는 결과와 맞게 보이도록 검색 후보 범위와 정렬 방식을 조정하는 것도 까다로웠습니다.”

### 아쉬웠던 점

“아쉬운 점은 아직 완전 자동 수집 배치나 운영 로그까지 완성된 상태는 아니라는 점입니다. 또한 신청 현황, 알림, 조회수, 전환율 같은 운영 기능은 구현된 기능으로 보기 어렵습니다. 챗봇도 대화 transcript 저장은 가능하지만, 저장된 과거 대화를 embedding으로 다시 검색하는 기능은 아직 없습니다.”

### GPU 활용 아쉬움

“GPU 지원이 가능했지만, 이번 프로젝트는 직접 모델을 학습하기보다는 정책 데이터를 수집·정제하고 Supabase DB와 Next.js 웹을 통해 검색, 추천, 챗봇 기능을 연결하는 서비스형 프로젝트에 가까웠습니다. 자체 임베딩 생성, 공고 분류 모델 학습, 검색 결과 리랭킹 모델 실험도 고려했지만, 프로젝트 기간 안에서는 전체 서비스 흐름을 완성하고 안정적으로 시연 가능한 상태로 만드는 것을 우선했습니다. 결과적으로 GPU 지원 자원을 충분히 활용하지 못한 점은 아쉬움으로 남았습니다.”

### 향후 개선 방향

“향후에는 수집과 업로드를 자동 배치화하고, 대상 조건을 표준 태그로 정규화해 필터 정확도를 높이고 싶습니다. 또 저장 대화의 의미 검색, 실제 운영 지표 수집, 알림 기능, GPU 기반 리랭킹 모델 실험까지 확장하면 서비스 완성도를 더 높일 수 있을 것입니다.”

## 11. 데모 시나리오 v2에 반영할 내용

### 데모에서 꼭 보여줄 기능

- 통합검색:
  - 추천 검색어: `서울 청년 창업 지원`
  - 보여줄 포인트: 검색어 embedding similarity, 지역/나이 보정, 카드 렌더링
- 조건검색:
  - 추천 필터 조합: 지역 `서울`, 분야 `창업`, 나이 `25세`
  - 보여줄 포인트: 지역/분야 필터, 나이 조건 보정, 조건 적합도 정렬
- 추천순/날짜 정렬:
  - 추천순은 `서울 청년 창업 지원`으로 보여주는 것이 안정적이다.
  - 날짜 정렬 비교는 서울+창업+25세 조합이 좋다.
- 공고 카드 더보기:
  - 지원 내용, 신청 방법, 제출 서류, 상세 URL을 보여준다.
- 스크랩:
  - 공고 스크랩 토글과 마이페이지 조회를 보여준다.
- AI 추천:
  - 프로필 또는 스크랩 기반 맞춤 추천으로 소개한다.
- 챗봇:
  - 프로필 조건과 질문 조건을 함께 고려하는 흐름을 보여준다.
- 관리자 화면:
  - read-only 통계 조회 화면으로 소개한다.

### 데모에서 숨기거나 짧게 넘길 기능

- 대상 필터:
  - 대부분 hard filter가 아니므로 길게 강조하지 않는다.
- 스크랩 추천 카드의 상태 문구:
  - “진행중” hardcode 가능성이 있으면 짧게 넘긴다.
- 관리자 수집 로그:
  - 화면에서 수집 로그 영역이 숨김 상태라면 말하지 않는다.
- 신청 현황:
  - 구현된 기능처럼 보여주지 않는다.
- 알림/조회수/전환율/MAU:
  - 향후 개선 방향으로만 언급한다.

### 문구를 조심해야 할 기능

- 통합검색 추천순:
  - “분야까지 직접 점수화”라고 말하지 않는다.
- 조건검색 추천순:
  - “AI 의미 검색”이라고 말하지 않는다.
- AI 추천:
  - “프로필과 스크랩을 항상 모두 반영”이라고 말하지 않는다.
- 챗봇:
  - “과거 대화를 embedding으로 검색”한다고 말하지 않는다.
- 관리자:
  - “정책 CRUD 관리”라고 말하지 않는다.
- 자동 수집:
  - “완전 자동 업데이트”라고 말하지 않는다.

### 추천 검색어

- `서울 청년 창업 지원`
- `청년 창업 지원`
- `서울 창업 지원`
- `전세사기 피해 청년 지원`

### 추천 필터 조합

- 지역 `서울` + 분야 `창업` + 나이 `25세`
- 지역 `전국` + 분야 `금융/생활지원` + 나이 `29세`
- 지역 `부산` + 분야 `취업` + 나이 `27세`

### 추천 챗봇 질문

- “서울에 사는 25세 청년이 신청할 수 있는 창업 지원 정책을 알려줘.”
- “내 조건에 맞는 청년 지원사업 중 마감이 가까운 걸 추천해줘.”
- “방금 추천한 공고 중 신청 방법이 간단한 것부터 설명해줘.”
- “전세사기 피해를 입은 청년이 볼 만한 지원 정책이 있어?”

### 피해야 할 검색어/정렬 조합

- 문장형 전세사기 검색어로 마감순/신규순 비교:
  - 전세사기 문장은 추천순만 보여주고 날짜 정렬 비교는 피한다.
- 너무 긴 자연어 질문 + 날짜 정렬:
  - 검색어 의미 후보가 좁게 잡힐 수 있어 데모 품질이 흔들릴 수 있다.
- 대상 필터만 단독으로 강조하는 조합:
  - 대상 필터는 대부분 hard filter가 아니므로 핵심 데모로 삼지 않는다.
- GitHub HEAD 기준에서 검색어 있는 날짜 정렬:
  - 상위 10개 안에서만 날짜 정렬될 수 있으므로, 로컬 데모 기준과 구분한다.

# 최종 요약

## 발표에서 꼭 강조할 점

- 실제 외부 정책 API 데이터를 수집하고 정규화해 Supabase에 저장한 end-to-end 서비스 흐름을 만들었다.
- 통합검색은 embedding similarity 기반이고, 조건검색은 사용자가 선택한 조건 적합도 중심이다.
- 공고 카드 더보기는 실제 DB 상세 필드와 연결되어 있다.
- 챗봇은 질문, 프로필 조건, 현재 대화 맥락을 함께 고려한다.
- 관리자 대시보드는 mock이 아니라 실제 DB 집계를 보여주는 read-only 화면이다.

## 발표에서 말하면 안 되는 것

- 완전 자동 수집/자동 업데이트가 구현되었다고 말하지 않는다.
- 대상 필터가 모든 대상 유형을 정확히 hard filter한다고 말하지 않는다.
- 통합검색에서 분야가 final_score에 직접 반영된다고 말하지 않는다.
- 저장 대화를 embedding으로 의미 검색한다고 말하지 않는다.
- 신청 현황, 조회수, 신청건수, 전환율, MAU, 알림이 구현되었다고 말하지 않는다.
- 관리자 화면에서 정책 추가/수정/삭제가 가능하다고 말하지 않는다.

## 데모 전에 문구 수정이 필요한 것

- 통합검색 추천순 tooltip:
  - “검색어 유사도와 적용된 필터 조건을 함께 참고해 관련 공고를 먼저 보여드려요.”
- 조건검색 추천순 tooltip:
  - “선택한 필터 조건에 맞는 공고를 조건 적합도순으로 보여드려요.”
- 스크랩 페이지 섹션명:
  - “맞춤 추천 공고”
- AI 추천 카드 설명:
  - “저장한 프로필 또는 스크랩 이력을 바탕으로 추천합니다.”
- 대상 필터 설명:
  - “대상 중 청년/시니어는 나이 조건으로 보조 반영됩니다.”
- 관리자 헤더:
  - “공고, 사용자 조건, 스크랩 집계를 조회 전용으로 확인합니다.”
- 추천 카드 상태:
  - 실제 상태 표시 또는 “진행중” 문구 제거

## PPT로 옮길 때 핵심 슬라이드 후보

- 문제 정의: 정책 정보는 많지만 내 조건에 맞는 공고를 찾기 어렵다.
- 전체 아키텍처: API 수집 → Python 정규화 → Supabase → Next.js API → 웹 화면
- 데이터 파이프라인: 온통청년/기업마당/K-Startup 수집과 정규화
- 검색 로직: 통합검색과 조건검색의 차이
- 추천 로직: 프로필/스크랩 기반 맞춤 추천
- 챗봇 로직: 질문, 프로필, 대화 맥락, 검색 결과 기반 답변
- 주요 화면: 검색, 더보기, 스크랩, AI 추천, 챗봇, 관리자
- 구현 회고: 잘 된 점과 한계
- 향후 개선: 자동화, 필터 정교화, 대화 의미 검색, 운영 지표, GPU 모델 실험

## 수정/개선 우선순위

| 우선순위 | 항목 | 이유 |
|---|---|---|
| P0 | 통합검색/조건검색 추천순 tooltip 수정 | 실제 정렬 로직과 화면 설명이 다르면 발표 중 질문을 받기 쉽다. |
| P0 | 대상 필터 설명 수정 | hard filter처럼 오해될 수 있다. |
| P0 | 스크랩 추천 섹션명 수정 | 프로필 추천이 섞일 수 있는 API와 문구가 어긋난다. |
| P0 | 추천 카드 “진행중” 상태 처리 | 실제 상태가 아니면 신뢰도 문제가 생긴다. |
| P1 | AI 추천 카드 설명 수정 | 항상 모든 조건을 쓰는 것처럼 보이면 부정확하다. |
| P1 | 관리자 헤더/설명 수정 | 수집 로그나 CRUD 기능처럼 오해되지 않게 해야 한다. |
| P1 | 로컬 데모 기준 확정 | GitHub HEAD와 로컬 동작 차이가 섞이면 발표 기준이 흔들린다. |
| P2 | 대상 태그 정규화 개선 | 장기적으로 필터 정확도를 높이는 핵심 개선이다. |
| P2 | 저장 대화 embedding 검색 추가 | 챗봇 개인화 품질을 높일 수 있다. |
| P2 | 자동 수집/업로드 배치화 | 운영 서비스로 발전시키기 위한 다음 단계다. |
