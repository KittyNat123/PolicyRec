# 📝 PolicyRec 서비스 데모 시나리오 정의서

## 1. 프로젝트 개요 및 데모 목적
*   **프로젝트 명**: PolicyRec (AI 기반 맞춤형 정책 큐레이션 플랫폼)
*   **데모 핵심 기능**: 
    1. AI(Gemini)와 하이브리드 검색을 통해 사용자에게 딱 맞는 정책을 찾아주는 **지능형 검색 엔진** 
    2. 사용자의 프로필과 실시간 활동(스크랩)을 결합한 **개인화 추천 시스템** 
    3. 복잡한 공고문을 읽지 않아도 핵심 정보를 바로 알려주는 **RAG 기반 대화형 상담** 

---

## 2. 데모 시나리오 상세 Flow

### [Scene 1] 지능형 검색 (자연어 및 조건 검색)
*   **1-1. 자연어 검색 (통합 검색)**:
    *   **액션**: 검색창에 "경기도 창업 지원 정책" 입력
    *   **포인트**: 지역/분야 일치하는 것 확인 
    *   **백엔드 로직**: 
        - Self-Query 필터 추출: AI(Gemini)가 사용자의 자연어 질문("경기도 창업 지원...")을 분석하여 지역: 경기도, 분야: 창업과 같은 논리적 필터 조건을 자동 추출.
        - 하이브리드 검색: 추출된 필터 조건과 질문의 벡터 유사도(Semantic Search)를 결합하여 DB에서 관련 공고 후보군을 1차 선별.
        - 지능형 리랭킹(Reranking): 서버 내 calculateFinalScore 함수가 유사도 점수에 사용자의 프로필/필터 정보(지역 +30점, 나이 +10점 등)를 합산하여 최종 순위를 재정렬.
        - 최종 정규화 및 필터링: normalizeResultRow를 통해 공고 데이터를 표준 규격으로 변환한 후, applyResultFilters로 최종 자격 요건을 재검증하여 사용자 화면에 출력.
*   **1-2. 조건 검색 (필터 검색)**:
    *   **액션**: '시설/공간', '인천광역시'
    *   **포인트**: 단순 마감순이 아닌, 서버에서 계산된 **Condition Score(지역/분야 가점)**에 따라 사용자와 가장 매칭률이 높은 공고가 상단에 배치되는 모습 시연
    *   **주의사항**: 필터에 완전 일치하는 데이터가 아닌 가중치에 따라 매칭률이 높은 순으로 출력되기 때문에, 선택 필터 정보와 일치하는 데이터가 상단에 노출되는 건으로 선택 필요
    *   **백엔드 로직**: 
        - 검색어(Query)가 없을 경우 searchByFiltersOnly 모듈이 작동하여 DB에서 필터와 일치하는 후보군을 1차 추출.
        - 서버 내 calculateConditionScore 함수를 통해 사용자가 선택한 필터와 공고의 일치 여부를 계산하여 가중치 점수(지역 30점, 분야 20점, 나이 10점) 부여.
        - 계산된 가중치 점수(condition_score) 내림차순으로 1차 정렬 후, 동일 점수 시 마감 임박순으로 2차 정렬하여 최적 매칭 공고를 상단에 노출.

### [Scene 2] 프로필 확인 및 스크랩 관리
*   **액션**: 
    1. 프로필 수정 페이지에서 현재 사용자의 정보(지역, 나이, 관심분야) 확인
    2. 검색 결과 중 유사 분야의 공고 3~4개를 연속으로 스크랩 (마감임박+마감넉넉한 공고 섞어서)
    * (참고사항 : 프로필 : 회원가입시 입력, 자주 수정하지 않음. 스크랩 : 활동에 따라 수시로 변함. 시연시 관심사에 따라 프로필과 다른 분야의 공고 스크랩.)
    3. 스크랩 페이지에서 스크랩한 공고들을 분야별 조회가능, **마감 임박순**으로 정렬하여 관리하는 편의성 확인

### [Scene 3] 개인화 맞춤 추천
*   **액션**: 메인 홈 화면으로 이동하여 추천 섹션 확인
*   **포인트**: 사용자의 고정 프로필 정보와 방금 Scene 2에서 수행한 **스크랩 활동(동적 데이터)**이 결합되어 실시간으로 최적화된 추천 리스트가 출력되는 모습 시연
*   **백엔드 로직**: 
    - 하이브리드 추천 엔진 작동: 사용자의 정적 정보(프로필의 지역, 나이)와 동적 활동 정보(최근 스크랩한 공고들의 임베딩 데이터)를 결합.
    - 스크랩된 공고의 벡터값(Vector)과 유사한 정책들을 DB에서 유사도 검색으로 찾아내어 사용자의 현재 관심사를 실시간 반영.
    - 프로필 정보(나이/지역) 가산점과 스크랩 유사도 점수를 합산하여 최종 추천 리스트 생성.

### [Scene 4] AI 대화형 상담 (RAG & Context Chat)
*   **액션**: 
    1. 챗봇에게 "내가 신청할 만한 부산 창업 정책 추천해줘"라고 질문
    2. 특정 공고를 지목하여 "**해당 공고** 신청방법(또는 마감일자 등 존재하는 데이터로 질문) 알려줘"라고 상세 내용 질문
*   **포인트**: 
    1. 공고명을 다시 입력하지 않아도 이전 대화를 기억하여 답변하는 **대화 맥락 유지(Memory)** 기능 시연
    2. 대화 중 특정된 공고에 대해 실시간으로 본문을 분석(RAG)하여 접수처, 신청 방법, 마감 기간 등 구체적 정보를 제공하는 과정 확인
*   **주의사항**: 
    1. 챗봇 검색시 접수처나, 마감일자, 신청방법등을 질문해도 질문에 대한 답변은 해주나, 맨 마지막에 공고 목록이 출력되는 현상이 발생. 감안하여 시연 필요합니다.
    2. 1번과 같은 맥락으로 질문을 이어서 하는 경우 공고 5건 출력(기본건수)이 계속 나오니 스크롤을 빨리 내리시면 좋을 듯 합니다ㅎㅎ
*   **백엔드 로직**:
    - 대화 이력 관리(Chat History): 이전 대화 내용을 세션에 저장해두었다가 다음 질문 시 AI(Gemini)에게 함께 전달하여 "그중에서", "해당 공고"와 같은 지시 대명사의 대상을 파악.
    - RAG (검색 증강 생성): 특정된 공고의 전체 본문 텍스트를 실시간으로 가져와 AI가 질문(접수방법, 서류 등)에 필요한 정보만 발췌 및 요약.